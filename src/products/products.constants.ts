/**
 * A product at or below this many units (but above zero) counts as "low
 * stock" — shared by the admin catalogue filters and the analytics
 * dashboard so both screens agree on the number.
 */
export const LOW_STOCK_THRESHOLD = 5;
